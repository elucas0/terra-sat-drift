[project]
name = "terra-sat-drift"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "terratorch>=1.2.4",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
